schema_version: "1.1"
baseline_id: win.scheduled_task.modify
version: "0.1.0"
title: Windows 计划任务修改遥测
description: 以本轮唯一任务路径、修改后 XML 内的 nonce 标记和精确行为时间关联 Windows 安全事件 4702；要求本地定义哈希确实变化。
platform:
  os: windows
  versions: [windows-10, windows-11, windows-server-2019, windows-server-2022]
  architectures: [x64]
risk_level: L1
capability:
  id: win.scheduled_task.modify
  version: "0.1.0"
  parameters: { post_operation_hold_ms: 1500 }
local_requirements:
  - { field: facts.scheduled_task.modify_succeeded, operator: equals, expected: true }
  - { field: facts.scheduled_task.task_path, operator: present }
  - { field: facts.scheduled_task.marker, operator: present }
  - { field: facts.scheduled_task.before.exists, operator: equals, expected: true }
  - { field: facts.scheduled_task.before.xml_sha256, operator: present }
  - { field: facts.scheduled_task.after.exists, operator: equals, expected: true }
  - { field: facts.scheduled_task.after.xml_sha256, operator: present }
  - { field: facts.scheduled_task.after.enabled, operator: equals, expected: false }
  - { field: facts.scheduled_task.after.action_arguments, operator: present }
  - { field: facts.scheduled_task.actor_pid, operator: present }
correlation:
  time_before_seconds: 60
  time_after_seconds: 300
  max_time_difference_ms: 15
  anchors:
    - { local_field: facts.scheduled_task.task_path, cloud_field: scheduled_task.name, strength: strong, normalizers: [lowercase, trim] }
    - { local_field: facts.scheduled_task.actor_executable, cloud_field: process.executable, strength: medium, normalizers: [windows_path] }
    - { local_field: facts.scheduled_task.actor_pid, cloud_field: process.pid, strength: medium }
cloud_expectations:
  - id: scheduled-task-update-event
    event_type: scheduled_task
    event_actions: [modify]
    cardinality: { min: 1, max: 5 }
    correlation:
      time_from_local: facts.scheduled_task.occurred_at_utc
      anchors:
        - { local_field: facts.scheduled_task.task_path, cloud_field: scheduled_task.name, strength: strong, normalizers: [lowercase, trim] }
        - { local_field: facts.scheduled_task.actor_executable, cloud_field: process.executable, strength: medium, normalizers: [windows_path] }
        - { local_field: facts.scheduled_task.actor_pid, cloud_field: process.pid, strength: medium }
    assertions:
      - { field: scheduled_task.name, operator: equals, expected_from_local: facts.scheduled_task.task_path, severity: required, normalizers: [lowercase, trim] }
      - { field: scheduled_task.content, operator: contains, expected_from_local: facts.scheduled_task.marker, severity: required }
      - { field: winlog.event_id, operator: equals, expected: 4702, severity: required }
      - field: process.executable
        operator: equals
        expected_from_local: facts.scheduled_task.actor_executable
        accepted_values:
          - { value: 'C:\Windows\System32\svchost.exe', message: EDR 仅保留 Task Scheduler 服务侧调用链，需要补充客户端调用链 }
        severity: recommended
        normalizers: [windows_path]
      - { field: user.id, operator: present, severity: recommended }
scoring: { weight: 1.0, required_field_weight: 1.0, recommended_field_weight: 0.25 }
metadata:
  owner: edr-validation-team
  status: active
  change_reason: 按真实 ScheduleTaskEvents 修改样本校准 TaskContentNew、4702，并允许 svchost.exe 服务侧调用链以同分通过推荐项。
  references: [reference/scheduled_task/edr_update.json, Windows Security Event 4702, Tencent EDR ScheduleTaskEvents / SchedTaskUpdate]
